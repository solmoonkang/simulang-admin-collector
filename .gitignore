# 의존성
node_modules

# 자격증명 (아이디·비밀번호·사이트주소) — 절대 커밋 금지
.env

# 캡처/임시 산출물
*.png
/tmp/

# 참고: 수집한 데이터(JSON)는 레포 '바깥'(OUTPUT_DIR, 기본 ~/collector-output)에 저장되어
#       애초에 레포 안에 존재하지 않는다. (gitignore에 의존하지 않는 구조적 차단)
